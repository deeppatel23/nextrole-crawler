# nextrole-crawler

```
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python3 main.py
deactivate
```

Local LLM
```
brew install ollama
ollama serve

ollama pull llama3
```